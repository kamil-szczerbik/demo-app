version: "3.8"
services:
 mysql:
  image: mysql
  ports:
   - 3306:3306
  volumes:
   - db_data:/var/lib/mysql
  env_file:
   - .env

 adminer:
  image: adminer
  ports:
   - 8080:8080

volumes:
 db_data:
  driver: local